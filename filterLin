package utils;
import org.json.simple.*;

import model.Lavori;
import java.util.*;
public class filterLin extends Filters {
	public filterLin(String[] city, String[] linguaggi,String remoto, String link, Date data,String oreLavoro){
		 super(city,linguaggi,remoto,link,data,oreLavoro);
	}
	public ArrayList<Lavori> filtraggio(String[] flin,Vector<Lavori> offerte) {
		ArrayList<Lavori> filtrato = new ArrayList<Lavori>();
		 for(Lavori z:offerte) {
			 List <String> l=z.getLinguaggi();
			 
			  {
			 for(String c:l) {
				 for(String c1:flin) {
					if(c.equals(c1)){
						  filtrato.add(z);
					 }
					 {
						 
				 
			 
				 }
					 
			 }}
		 }
		 
	 }
		 return filtrato;

}}
